{
  "name": "demini-test-fixture",
  "version": "1.0.0",
  "private": true,
  "description": "Synthetic test bundle for demini pipeline validation — known source as experimental control",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "esbuild src/index.js --bundle --minify --outfile=dist/bundle.js --platform=node --format=esm",
    "start": "node src/index.js",
    "start:bundle": "node dist/bundle.js"
  },
  "dependencies": {
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
